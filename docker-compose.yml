services:
  postgres:
    image: postgres:17-alpine
    restart: unless-stopped
    environment:
      POSTGRES_DB: mound
      POSTGRES_USER: mound
      POSTGRES_PASSWORD: mound
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U mound"]
      interval: 5s
      timeout: 5s
      retries: 5

  app:
    build:
      context: .
      dockerfile: Dockerfile
    restart: unless-stopped
    ports:
      - "80:8080"
    depends_on:
      postgres:
        condition: service_healthy
    environment:
      DB_DSN: postgres://mound:mound@postgres:5432/mound?sslmode=disable
      PORT: "8080"
      JWT_SECRET: change-me-in-production

volumes:
  postgres_data:
